<Window x:Class="DevelopmentTools.Modules.SheetTools.SheetDuplicator.SheetDuplicatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="圖紙逐層量化開圖" Height="650" Width="700"
        Background="#1E1E2E" Foreground="#CDD6F4" FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#181825"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="RowBackground" Value="#181825"/>
            <Setter Property="AlternatingRowBackground" Value="#1E1E2E"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#BAC2E0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#45475A"/>
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#89B4FA"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="10,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📑 圖紙逐層量化開圖" FontSize="18" FontWeight="Bold" Foreground="#CBA6F7" Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="輸入圖紙編號前綴：" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding SheetPrefix, UpdateSourceTrigger=PropertyChanged}" Height="26" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
            <Button Grid.Column="2" Content="分析" Command="{Binding AnalyzeCommand}" Width="60" Height="26" Style="{StaticResource PrimaryBtn}" Background="#89B4FA"/>
        </Grid>

        <GroupBox Grid.Row="2" Header="視圖內容分析表" Foreground="#A6E3A1" BorderBrush="#45475A" Margin="0,0,0,15">
            <DataGrid ItemsSource="{Binding AnalyzedViews}" Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="來源圖紙" Binding="{Binding SourceSheetNumber}" Width="*"/>
                    <DataGridTextColumn Header="視圖名稱" Binding="{Binding ViewName}" Width="2*"/>
                    <DataGridTextColumn Header="類型" Binding="{Binding ViewType}" Width="*"/>
                    <DataGridTextColumn Header="關聯樓層" Binding="{Binding LevelName}" Width="*"/>
                    <DataGridTextColumn Header="使用樣板" Binding="{Binding TemplateName}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="選擇目標樓層" Foreground="#F9E2AF" BorderBrush="#45475A" Margin="0,0,0,15" MaxHeight="150">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding TargetLevels}" Margin="5">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" Margin="0,4">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" Foreground="#CDD6F4" Width="100"/>
                                    <TextBlock Text="{Binding ElevationInfo}" Foreground="#6C7086"/>
                                </StackPanel>
                            </CheckBox>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <Grid Grid.Row="4" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="執行量化開圖" Command="{Binding GenerateCommand}" Style="{StaticResource PrimaryBtn}" Background="#A6E3A1" Margin="0,0,10,0"/>
            <Button Grid.Column="1" Content="關閉" Command="{Binding CloseCommand}" Style="{StaticResource PrimaryBtn}" Background="#45475A" Foreground="#CDD6F4" Width="100"/>
        </Grid>

        <TextBlock Grid.Row="5" Text="{Binding StatusText}" Foreground="#89B4FA" FontSize="11" TextWrapping="Wrap"/>
    </Grid>
</Window>
